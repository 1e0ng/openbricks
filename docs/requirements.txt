# Toolchain for building docs.openbricks.dev (Sphinx).
# CI installs exactly this; build locally with:
#   pip install -r docs/requirements.txt
#   sphinx-build -W --keep-going -b html docs docs/_build/html
sphinx>=7.0
sphinx-rtd-theme>=2.0
myst-parser>=3.0
sphinx-argparse>=0.6.1
sphinx-sitemap>=2.6
