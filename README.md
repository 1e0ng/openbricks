# docs-archive

Orphan branch holding the frozen docs build (HTML + `openbricks-docs.pdf`)
of every released firmware version, one `X.Y.Z/` directory per release.
Written by `.github/workflows/website.yaml` (v* tag pushes and the
backfill dispatch); merged into the docs.openbricks.dev Pages artifact
on every deploy. Never edit by hand.
